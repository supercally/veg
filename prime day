#include<stdio.h>
int zhishu(int i);
int panduan(int n);
int operate(int n);
int shaixuan(int i);
int main()
{
	int m,n,i,flag;
	scanf("%d",&m);
	scanf("%d",&n);
	for(i=m;i<=n;i++)
	{
	    flag=zhishu(i);/*是否满足素数日要求*/ 
		if(flag==1)
		    if(shaixuan(i)==1)/*筛选日期*/
		        printf("%d\n",i); 
	}    
	return 0;
}
int zhishu(int i)
{
	int flag,n;
	for(n=i;n!=0;n=operate(n))/*去掉第一个数字*/ 
	{   
	    flag=panduan(n);/*判断是否为素数*/ 
		if(flag==0)
		    return 0;
	}
	return 1;
}
int panduan(int n)
{
	int i,j;
	for(i=2;i<n;i++)
	    if(n%i==0)
	        return 0;
	return 1;
}
int operate(int n)
{
	int i,m;
	for(i=1,m=n;m/i!=0;i=i*10);
	i=i/10;
	return n%i;
}
int shaixuan(int i)
{
	int j=i%10000;
	if((j>=101&&j<=131)||(j>=201&&j<=229)||(j>=301&&j<=331)||(j>=401&&j<=430)||(j>=501&&j<=531)||(j>=601&&j<=630)||(j>=701&&j<=731)||(j>=801&&j<=831)||(j>=901&&j<=930)||(j>=1001&&j<=1031)||(j>=1101&&j<=1130)||(j>=1201&&j<=1231))
	    return 1;
	else
	    return 0;
}
